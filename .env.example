# API keys and environment configuration for EOH.
# Copy to `.env` and fill in values before running live data downloads.

ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key
OPENAI_API_KEY=your-openai-key
LLM_MODEL_DIR=/path/to/local/hf/model
DATA_CACHE_DIR=./data/cache
